{
  "name": "adesso-input",
  "version": "0.0.18",
  "description": "",
  "main": "./dist/main.js",
  "scripts": {
    "test": "jest",
    "build": "webpack --mode='development'",
    "publish": "webpack --mode='production' && git add . && git commit -m 'classic comment' && git push && npm version patch && npm publish"
  },
  "peerDependencies": {
    "validator": "^10.9.0"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.1.6",
    "@babel/preset-env": "^7.1.6",
    "babel-loader": "^8.0.4",
    "jest": "^23.6.0",
    "webpack": "^4.26.0",
    "webpack-cli": "^3.1.2"
  },
  "dependencies": {
    "validator": "^10.9.0"
  }
}
